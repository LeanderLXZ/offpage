# 架构详情文档 <!-- holo:heading -->

<!-- holo:section start -->
每个架构关注点单独一个文件（例如 `data-flow.md`、
`auth-model.md`、`branch-strategy.md`）。第一个关注点出现时再创建；
在此之前本目录有意保持为空。

`ai_context/architecture.md` 是指向这里的压缩索引 ——
参见其 `## Top-Level Structure` / `## System Layers` / `## Key
Boundaries` / `## Runtime / Entry Points` 各段。
<!-- holo:section end -->
