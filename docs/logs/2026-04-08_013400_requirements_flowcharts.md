# 需求文档流程图补充

**日期**: 2026-04-08
**范围**: 需求文档可读性增强

## 背景

需求文档中多个关键流程仅有纯文字描述，缺乏可视化。为各核心章节补充
ASCII 流程图，提升可读性和快速理解效率。

## 新增流程图

### §5 用户流程与会话生命周期

用户入口（新用户/已有用户分支）→ 设定锁定 → Context 生命周期
（对话实时更新 → 正常关闭/异常中断 → 合并/不合并 → 归档）。
覆盖完整的 context 状态流转：ephemeral → persistent → merged。

### §7 信息分层与加载原则

两部分：
1. **信息五层图**：不变层 → 阶段快照 → 历史记忆 → 对话可变层 →
   跨对话积累层，每层标注加载时机和内容
2. **四级加载策略图**：Tier 0 启动核心 → Tier 1 结构化按需 →
   Tier 2 对话记录 → Tier 3 原文验证，纵向递进

### §9.4 自包含快照生成规则

展示从 Phase 2.5 baseline 产出 → batch 1 生成 stage 1 snapshot →
batch 2 参照 baseline + stage 1 生成 stage 2 → ... → batch N 的
完整数据流向和继承关系。底部标注运行时只加载快照、不加载 baseline。

### §10 稀释保护

两部分：
1. **运行时对话时间轴**：A（锚点重注入，每轮）、B（滚动状态，每 5-8 轮）、
   C（深度校准，每 15-20 轮或重大事件后）在对话轮次上的触发频率
2. **提取阶段 batch 前后检查**：batch 开始前的 3 项准备（重读 schema、
   重读架构规则、加载前批输出）和完成后的质量自检

### §12.2 记忆系统整体加载与检索

（上一条 log 的 memory_digest 变更中已部分涉及，此次补充完整的
启动加载 + 对话检索两阶段总览图，覆盖三层记忆 + 检索基础设施 +
两级漏斗的完整流程）

## 修改文件

- `docs/requirements.md` — §5, §7, §9.4, §10, §12.2 各增加一个流程图
