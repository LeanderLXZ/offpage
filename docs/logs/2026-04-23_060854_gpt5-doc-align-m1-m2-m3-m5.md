# 2026-04-23 06:08 — gpt-5 audit M1 + M2 + M3 + M5(A)：文档 / CLI 对齐

## 背景

gpt-5 报告四条 Medium，按推荐方案一次合并落地。其中 M4（current_status.md
过期）早前由 opus followup 已经 commit；本轮不重复处理。

## M1 — simulation/contracts/baseline_merge.md 运行时加载契约修正

原"`memory_timeline × stages 1..N` | Tier 0 startup (historical
memories)" 与 requirements / load_strategy 的"近期 2 阶段 full + 远端
digest + FTS5" 冲突。改动（同文件单一表格）：

- 近期 2 阶段（N + N-1）`memory_timeline/{stage_id}.json` 走 Tier 0
- `memory_digest.jsonl`（stage 1..N）Tier 0
- `world_event_digest.jsonl`（stage 1..N）Tier 0
- 较早阶段 `memory_timeline` 走 FTS5 / embedding 按需加载
- 过去 `stage_snapshots/{stage_id}.json` 按需加载

## M2 — CLI `--concurrency` 默认按 `--start-phase` 派发

原 `default=cfg.phase0.concurrency` 固定；help 文案写 "from phase0 /
phase4" 但 Phase 4 standalone 实际只拿 phase0 值。改动（`cli.py`）：

- `default=None` + help 文案说明"--start-phase 4 → phase4.concurrency；
  其他 → phase0.concurrency；显式传值覆盖"
- `argparse.parse_args` 后根据 `args.start_phase` 决定默认值；显式
  `--concurrency X` 直接跳过派发逻辑

## M3 — ai_context/architecture.md config override 顺序修正

原：`CLI flag > env > config.toml > config.local.toml`
改：`CLI flag > config.local.toml > config.toml > dataclass defaults`
（与 requirements §11 + `decisions.md §45` + `config.py` 实现对齐；
去除多余的 env 层描述——代码里无 env override 路径）

## M5(A) — data_model.md 世界目录"必需"→"计划"

原"必需内容"把 `history/timeline.jsonl` / `events/*` / `locations/*` /
`factions/*` / `maps/*` / `cast/*` / `state/world_state_snapshots/*`
列为必需，但对应 schema 尚未补齐。改动（`docs/architecture/data_model.md`
世界观包小节）：

- "必需内容（已 schema-gated）"：只留已有 schema 的 manifest / stage_catalog /
  stage_snapshots / foundation / fixed_relationships；在条目里补标 schema 路径
- "计划内容"：`history / events / locations / factions / maps / cast / state/*`
  列为计划，注明"schema 未补齐前不是硬门控必需；schema 补全后再升级为必需"
- 指向 `ai_context/next_steps.md` 里段 "Refine schemas into directly
  writable instance formats" 为对接点

## 验证

- `python -c 'from automation.persona_extraction import cli, config'` 通过
- CLI 派发逻辑：`--start-phase 4` + 不传 `--concurrency` → 应取
  `cfg.phase4.concurrency`；其他 → `cfg.phase0.concurrency`；显式
  `--concurrency N` → 用 N（逻辑顺序验证，未端到端跑）

## 未做 / 推迟

- M4（current_status.md 过期）早前由 `fea0384` 已 commit
- 彻底补齐世界层 schema（events / locations / factions / maps / cast /
  history / state）属于 next_steps 中期任务，不在本轮
- `baseline_merge.md` "Extraction Workflow" 章节的其他段落保留原貌

## 受影响文件清单

```
ai_context/architecture.md
automation/persona_extraction/cli.py
docs/architecture/data_model.md
simulation/contracts/baseline_merge.md
```
