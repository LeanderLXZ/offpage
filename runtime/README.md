# Runtime

This directory stores compiled runtime context and prompt assets.

Planned layout:

```text
runtime/
  compiled_context/
  prompt_templates/
```

Runtime output should be treated as derived state, not canonical truth.

Canonical truth lives in:

- `sources/`
- `characters/`
- `users/`
