"""Phase-specific extraction logic — scene archive (Phase 4), snapshot merge (Phase 3), post-processing (Phase 3).

Each module owns the artifact-generation work for its specific phase.
Phase-boundary validation lives under ``extraction.validation``.
"""
