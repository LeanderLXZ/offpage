# post_check_fixes_m1_l1_l2_l3

- **Started**: 2026-05-12 11:03:23 EDT
- **Branch**: main
- **Status**: PRE

## 背景 / 触发

上一轮 /post-check（commit `14b7dca` 回写 REVIEWED-PASS，intent log
`logs/change_logs/2026-05-12_091748_char_snapshot_sub_lanes.md`）发现 1
项 Medium + 4 项 Low Findings。用户指示"H1 L1 L2 L3 修复，但是不要过度
工程 /go"——把"H1"读作 M1（/post-check 报告里只有 1 项 Medium 编号 M1，
无 H 项；上下文显然指 M1+L1+L2+L3 四项；L4 ai_context/requirements.md
按 /post-check 建议**保持不补**）。

## 结论与决策

四项最小修复，**严守"不过度工程"**——每项不超过几行编辑：

- **M1**：`automation/persona_extraction/snapshot_merge.py:127-130` 的
  `OPTIONAL_TOP_LEVEL_FIELDS` 注释把 `chapter_scope` 列入"sub-lane
  partial 可省"语义，但 `chapter_scope` 实际是 `PROGRAM_INJECTED_FIELDS`，
  sub-lane 根本不写它。修法：删注释中的 `chapter_scope` 字面（保留其
  余 6 个真正属于 OPTIONAL_TOP_LEVEL_FIELDS 的字段名）。代码 frozenset
  不动。
- **L1**：`docs/architecture/schema_reference.md` 的
  `character/stage_snapshot.schema.json` 段（line 295-343）缺"生成方式"
  说明（同段位的 `world/world_stage_catalog`、`world/world_stage_snapshot`、
  `character/stage_catalog` 都有该小节）。修法：在 §"自包含契约（schema
  硬门控）"段之后补一段"生成方式"，简短描述 sub-lane merge 路径 +
  fallback 单 lane 路径 + 指向决策 #55，5-7 行。
- **L2**：`works/README.md` 描述 stage_snapshots 时未提及 `.partial/`
  子目录。修法：在对应路径条目末加一行括注"（sub-lane merge 中间产物
  暂存于 `.partial/` 子目录，phase 3 自动清理，`.gitignore` 屏蔽）"。
- **L3**：`schemas/README.md` 的 `character/stage_snapshot.schema.json`
  条目未交代产出方式。修法：条目描述末加一行括注"（phase 3 产出由
  `snapshot_merge.merge_partials` 合并 3 sub-lane partial，决策 #55）"。

不做：
- 不补 ai_context/requirements.md §11 sub-lane mention（L4，按
  /post-check 建议保持 "shorter is better" 原则）
- 不动 snapshot_merge.py 代码本身（M1 仅改注释）
- 不动 stage_snapshot.schema.json schema 内容
- 不动 sub-lane 实现 / orchestrator / merge / prompt template / config /
  cli / repair_agent
- 不引入新 decision 编号（沿用 #55）

## 计划动作清单

- file: `automation/persona_extraction/snapshot_merge.py` → 注释行 130 删
  `chapter_scope` 字面
- file: `docs/architecture/schema_reference.md` →
  `character/stage_snapshot.schema.json` 段补一段"生成方式"小节，5-7 行
- file: `works/README.md` → stage_snapshots 路径条目加一行 `.partial/`
  括注
- file: `schemas/README.md` → `character/stage_snapshot.schema.json` 条
  目末加一行产出方式括注
- file: `logs/change_logs/2026-05-12_110323_post_check_fixes_m1_l1_l2_l3.md`
  → 本文件（PRE + POST 同文件）

## 验证标准

- [ ] `python -c "from automation.persona_extraction.snapshot_merge import
      OPTIONAL_TOP_LEVEL_FIELDS, PROGRAM_INJECTED_FIELDS; assert
      'chapter_scope' not in OPTIONAL_TOP_LEVEL_FIELDS; assert
      'chapter_scope' in PROGRAM_INJECTED_FIELDS; print('ok')"` — 集合
      划分仍然正确（M1 只改注释，运行时不变）
- [ ] `grep -n 'chapter_scope' automation/persona_extraction/snapshot_merge.py`
      的注释段（行 127-141）中**不再**出现 `chapter_scope`（M1）
- [ ] `grep -n 'sub-lane\|sub_lane\|snapshot_merge\|决策 #55\|decision #55'`
      在 `schemas/README.md` / `works/README.md` /
      `docs/architecture/schema_reference.md` 各命中至少一处（L1 / L2 / L3）
- [ ] grep `旧 / legacy / 已废弃 / 原为` 在本次新增段落残留 0
- [ ] 全部 import smoke：snapshot_merge / prompt_builder / orchestrator
      仍可 import（M1 是注释改动，不可能破，但例行跑一次）
- [ ] todo_list 维护：本次 4 项 Findings 均不在 docs/todo_list.md，无需
      移段；ai_context/decisions.md 不新增 entry（不引入 durable 决策，
      仅 doc cosmetic 修复）

## 执行偏差

无。

## 已落地变更

- [automation/persona_extraction/snapshot_merge.py:127-141](automation/persona_extraction/snapshot_merge.py#L127) — `OPTIONAL_TOP_LEVEL_FIELDS` 注释段：删除 `chapter_scope` 字面，新增一句解释 `chapter_scope` 也是 schema 层 non-required 但属于 PROGRAM_INJECTED_FIELDS（sub-lane 不写它），故不应列入本集合。frozenset 内容 **未变**。
- [docs/architecture/schema_reference.md](docs/architecture/schema_reference.md) `character/stage_snapshot.schema.json` 段：在"自包含契约（schema 硬门控）"段之后新增"生成方式"小节（5 行），描述 `[phase3].char_snapshot_sub_lanes = true` 时由 `snapshot_merge.merge_partials` 合并 3 sub-lane partial 产出，开关关闭走单 LLM call fallback，schema 本身不感知该拆分（→ 决策 #55）。
- [works/README.md](works/README.md) `stage_snapshots/{stage_id}.json` 路径条目：增 2 行注释说明 phase 3 sub-lane 模式产出，中间产物落 `.partial/{stage_id}_{lane}.json` 由 phase 3 自动清理 + `.gitignore` 屏蔽（→ 决策 #55）。
- [schemas/README.md](schemas/README.md) `character/` 行的 `stage_snapshot` 条目：括注 phase 3 产出由 `snapshot_merge.merge_partials` 合并（→ 决策 #55）。
- [logs/change_logs/2026-05-12_110323_post_check_fixes_m1_l1_l2_l3.md](logs/change_logs/2026-05-12_110323_post_check_fixes_m1_l1_l2_l3.md) — 本文件，PRE + POST 同文件。

`git diff --stat` 实际行数：4 文件，+19 / −4 行。

## 与计划的差异

无。PRE 计划动作清单 4 项 + log 文件本身共 5 项全部落实，每项均按计划范围（≤10 行编辑）执行，未越界。

## 验证结果

- [x] `'chapter_scope' not in OPTIONAL_TOP_LEVEL_FIELDS and 'chapter_scope' in PROGRAM_INJECTED_FIELDS` — Python 断言通过；OPTIONAL 集合 6 项不含 chapter_scope，PROGRAM_INJECTED 7-tuple 第 7 位为 chapter_scope
- [x] `chapter_scope` 在 snapshot_merge.py 注释段（行 127-141）仅 1 处出现（新加的"is also non-required ... but is **program-injected**"行）—— 之前的"sub-lane partial 可省"列表里**不再出现**
- [x] `sub-lane | sub_lane | snapshot_merge | 决策 #55 | decision #55` 在 schemas/README.md（1 处）/ works/README.md（3 处）/ docs/architecture/schema_reference.md（5 处）各命中至少 1 处
- [x] `旧 / legacy / 已废弃 / 原为` 在本次 4 个新增段落 grep 残留 0（works/README.md:197 命中的"已废弃"是 decision #54 历史段落，**pre-existing**，与本次 4 项 fix 无关）
- [x] import smoke：`snapshot_merge` / `prompt_builder` / `orchestrator` 全部 import 无报错
- [x] todo_list / decisions.md 未触：本次是 /post-check Findings 的 cosmetic 修复，不涉及 todo 条目移段也不新增 durable 决策

## Completed
- **Status**: DONE
- **Finished**: 2026-05-12 11:08:10 EDT
