# 2026-04-20 15:15:39 — canonical docs 去具体名 + 去历史定语

## 背景

opus-4-7 于 2026-04-20 14:39 的 full repo alignment audit 指出：

- **H1（High）**：schemas / docs/requirements.md 里至少 6 处 description
  写死了具体作品的人物 / 地名，违反 user memory
  `feedback_no_specific_refs_in_docs.md`；且 schema description 会在
  每次 lane 调用时被 LLM 当作指令读取，污染能力更强。
- **M3（Medium）**：`automation/prompt_templates/character_snapshot_extraction.md`
  在字段命名对照表里写 "已移除，用 target_behavior_map"，违反
  `feedback_docs_describe_current_only.md`，且技术上不准确
  （`relationship_behavior_map` 在 baseline `behavior_rules.json` 仍是
  有效字段，只是 stage_snapshot 层另用 `target_behavior_map`）。

复核（/review-check）确认两条真实存在；额外补充扫描发现
`docs/requirements.md:1921` 有 "**移除的旧行为**" 段，同一根因。

Q3（规则化）也在本轮合并：把"canonical docs 必须用中性占位符 + 不写
历史定语"写进 `ai_context/conventions.md` 作为永久规则，防未来漂移。

## 改了什么

### H1 — 具体名改中性占位符

| 文件 | 行 | 改动 |
|------|----|------|
| `schemas/stage_snapshot.schema.json` | 620 | `萧尘为她挡剑` → `某关键他人的自我牺牲行为` |
| `schemas/stage_snapshot.schema.json` | 635 | `对萧尘从戒备转为信任` → `对某重要他人从戒备转为信任` |
| `schemas/stage_snapshot.schema.json` | 639 | `萧尘放弃帝位陪她渡劫` → `关键他人做出重大自我牺牲的行为` |
| `schemas/stage_snapshot.schema.json` | 667 | `从完全冰冷的前大帝……` → `从初始封闭/戒备状态，经关键他人持续的善意接触，逐步转向选择性信任与依赖` |
| `schemas/identity.schema.json` | 106 | `五岁被亲生父母抛弃于冰宫门前` → `幼年被至亲遗弃于某处` |
| `docs/requirements.md` | 2314 | `char_snapshot:王枫` → `char_snapshot:<character_id>` |
| `automation/persona_extraction/git_utils.py` | 134 | docstring `阶段01_南林初遇` → `阶段XX_<slug>` |

### M3 — prompt 模板"已移除"改为职责分离表述

`automation/prompt_templates/character_snapshot_extraction.md:106`：

旧：`| stage_snapshot | relationship_behavior_map | 已移除，用 target_behavior_map |`

新：`| stage_snapshot.behavior_state | relationship_behavior_map | stage 快照层用 target_behavior_map；relationship_behavior_map 仅在 baseline behavior_rules.json 使用 |`

既保留防坑意图，又不误导 LLM "该字段已废弃"。

### 报告外补充 — docs/requirements.md 的"**移除的旧行为**"段

`docs/requirements.md:1921-1923`（§11.5 lane-level resume 流程）整段删除。该段描述
`rollback_to_head` 被移除，但 §11.5 前文已把当前的
"lane_states + JSON 校验 resume" 流程描述完整，移除段属于冗余历史叙述。
历史在 git + docs/logs/ 已归档。

### Q3 — conventions.md 新增"Generic Placeholders in Canonical Docs"

`ai_context/conventions.md` 在 "Naming and Identifiers" 和 "Data Separation"
之间新增一节，明确：

- `schemas/`、`docs/requirements.md`、`docs/architecture/`、`ai_context/`、
  `prompts/`、`automation/prompt_templates/` 不得出现真实书名 / 角色 /
  地名 / 剧情。
- schema `description` 的 example 只写结构性描述，不写具体剧情。
- 字段举例用 `<character_id>` / `<stage_id>` / `阶段XX_<slug>` 等占位符。
- 不写 "旧 / legacy / 已废弃 / 原为 / 已移除 / renamed from"；历史
  归 `docs/logs/` + git。
- 例外（历史即目的）：`docs/logs/`、`docs/review_reports/`、
  `works/*/` 样例、git commit message。

## 为什么

- H1：规则级硬错；schema description 污染能力强。批量中性化一次到位。
- M3：文字错 + 语义错双重问题，一行解决。
- 补充段：同根因扩散，不单列 commit。
- Q3：把 user memory 的隐规则显性化为仓库规则，让 Codex / 人工审核
  也能看到，减少未来漂移。

## 校验

- `python -c "import json; json.load(...)"` 对两个 schema 成功。
- `Draft202012Validator.check_schema(...)` 对两个 schema 成功。
- `from automation.persona_extraction import git_utils` import 成功，
  docstring 已更新。
- 全仓 `grep -E '萧尘|冰宫|九世孽缘|姜寒汐|南林|王枫'`：
  命中仅剩 `works/`（样例数据，保留）、`docs/logs/`（历史）、
  `docs/review_reports/`（历史）。canonical docs / schemas / prompts /
  ai_context 全部干净。

## 不做

- 不动 `works/*/` 下的样例产出（那是真实 extraction 结果）。
- 不动 `docs/logs/` 和 `docs/review_reports/`（历史归档）。
- 不动 `schemas/behavior_rules.schema.json` 的 `relationship_behavior_map`
  字段结构（它在 baseline 有效，复核确认非冲突）。
