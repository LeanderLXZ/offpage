# Analysis

This directory stores incremental extraction outputs and supporting evidence.

Planned layout:

```text
analysis/
  incremental/
  evidence/
```

Recommended use:

- `incremental/` stores per-ingestion update packets.
- `evidence/` stores quote references, contradiction notes, and traceable
  support for character conclusions.
