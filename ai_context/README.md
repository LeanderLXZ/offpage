# AI Context

Compressed handoff for future AI sessions. Not human-facing project docs.

Read `instructions.md` first — it lists the reading order and logging
rules. Dilution protection (re-read triggers) lives in `CLAUDE.md` /
`AGENTS.md` as a self-check.

Only load heavier layers (`docs/logs/`, `docs/review_reports/`,
`docs/architecture/`, raw sources) when the task directly requires it.
